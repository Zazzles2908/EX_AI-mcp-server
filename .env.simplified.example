# === EX-AI MCP Server - Simplified Configuration (Draft) ===
# This is a simplified template aligned to the external review cleanup plan.
# Do NOT put secrets here. Copy to .env and fill in values when ready.

# === CORE PROVIDER KEYS (Required) ===
KIMI_API_KEY=
GLM_API_KEY=

# === MODEL DEFAULTS ===
DEFAULT_MODEL=auto
KIMI_DEFAULT_MODEL=kimi-k2-0905-preview
GLM_DEFAULT_MODEL=glm-4.5-flash
GLM_QUALITY_MODEL=glm-4.5
KIMI_QUALITY_MODEL=kimi-k2-0905-preview

# === CORE FEATURES ===
ENABLE_AI_MANAGER=true
ENABLE_WEB_SEARCH=true
ENABLE_FILE_UPLOAD=true
ENABLE_FALLBACK=true

# === TIMEOUTS (Seconds) ===
TOOL_TIMEOUT=120
PROVIDER_TIMEOUT=60
UPLOAD_TIMEOUT=180

# === SERVER CONFIG ===
MCP_SERVER_NAME=exai
EXAI_WS_HOST=127.0.0.1
EXAI_WS_PORT=8765
LOG_LEVEL=INFO

# === OPTIONAL PROVIDERS ===
OPENROUTER_API_KEY=
CUSTOM_API_URL=

# === ADVANCED (Optional) ===
MAX_FILE_SIZE_MB=100
MAX_CONTEXT_TOKENS=128000
ENABLE_CACHING=true
CACHE_TTL_HOURS=24

# === Moonshot (Kimi) OpenAI-compatible base_url ===
# Use example only, do not include keys in public examples.
OPENAI_BASE_URL=https://api.moonshot.ai/v1

# Notes:
# - This file is a draft target. Use scripts/migrate_config.py to generate .env.new from your current .env.
# - Keep your working .env minimal; .env.example holds authoritative guidance.

